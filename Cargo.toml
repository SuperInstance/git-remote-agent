[package]
name = "git-remote-agent"
version = "0.1.0"
edition = "2021"
description = "Git remote helper for Cocapn agents — push branches, pull commits back"
license = "MIT"

[dependencies]
serde_json = "1"
